apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.grafana.name }}-dashboard-config
  namespace: {{ .Values.namespace }}
data:
  coin-board.json: |-
{{ .Files.Get "configs/grafana/coin-board.json" | indent 4 }}
  logs.json: |-
{{ .Files.Get "configs/grafana/logs.json" | indent 4 }}
  dashboards.yaml: |-
{{ .Files.Get "configs/grafana/dashboards.yaml" | indent 4 }}